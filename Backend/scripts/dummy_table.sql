CREATE SCHEMA `learning_curve_schema` ;


CREATE TABLE `learning_curve_schema`.`test_table` (
                                                      `ID` INT NOT NULL AUTO_INCREMENT,
                                                      `NAME` VARCHAR(45) NULL,
                                                      PRIMARY KEY (`Id`));